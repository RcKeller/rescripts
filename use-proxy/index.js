// NOTE: WIP
const { getPaths, edit } = require('@rescripts/utilities')

const webpack = (config, options = {}) => {
  return config
}

const devServer = (config) => {
  return config
}

const jest = (config) => {
  return config
}

module.exports = (options) => { webpack, devServer, jest }
